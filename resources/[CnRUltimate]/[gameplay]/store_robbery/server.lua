

-- local function CreatePeds()

--     for i, store in pairs(Config.Stores) do
        
--         local pos = store.position
--         store.ped = CreatePed(4, "mp_m_shopkeep_01", pos.x, pos.y, pos.z, 0.0, false, false)
--         FreezeEntityPosition(store.ped, true)
--     end

-- end

-- CreatePeds()
